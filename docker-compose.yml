version: '3'
services:
  server:
    env_file: .env
    build: .
    volumes:
      - .:/usr/src/match-three
      - /usr/src/match-three/node_modules
    ports:
      - "${PORT}:${PORT}"
      - "9000:9000"
